exports.path = {
    root: __dirname,
    lib: __dirname + '/lib',
    processor: __dirname + '/processor',
    config: __dirname + '/config',
    logs: __dirname + '/logs',
    test: __dirname + '/test'
}